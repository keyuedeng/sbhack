# Shared Resources

Shared TypeScript types and scenario definitions used across services.

## Structure

```
types/                # TypeScript interfaces (Patient, Session, Scenario, etc.)
scenarios/            # Medical scenario definitions (JSON/YAML)
```

## Purpose

- Ensure type consistency across frontend and backend
- Centralize scenario configuration
- Define data contracts between services
